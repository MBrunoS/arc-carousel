{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky install",
    "pre-commit": "pnpm run --filter core pre-commit"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pnpm pre-commit"
    }
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "eslint": "^7.32.0",
    "husky": "^8.0.3",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.2",
  "name": "arc-carousel",
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
